fastapi==0.109.0
# Pin starlette: fastapi 0.109 requires <0.36; without this pin the agent SDK's
# transitive deps (mcp/sse-starlette) pull starlette 1.x and break Router init
# with: TypeError: Router.__init__() got an unexpected keyword argument 'on_startup'.
starlette==0.35.1
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
pymysql==1.1.0
cryptography==42.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# python-multipart / pydantic(-settings) raised to satisfy mcp + openai-agents,
# which need pydantic>=2.7. Verified working with fastapi 0.109.
python-multipart>=0.0.9
pydantic>=2.7,<3
pydantic-settings>=2.1.0
# httpx capped <0.28: 0.28 dropped the `app=` shortcut used by starlette 0.35 TestClient.
httpx>=0.26,<0.28
openai>=1.30.0
python-dotenv==1.0.0
aiohttp==3.10.5
openai-agents>=0.0.19
mcp>=1.2.0
# mcp pulls sse-starlette; 2.x+/3.x require starlette>=0.49 which conflicts with
# fastapi 0.109's starlette<0.36. We only use MCP over stdio, so <2 is sufficient.
sse-starlette<2
trafilatura==2.1.0
selectolax==0.4.10