services:
  mysql:
    image: mysql:latest
    container_name: ai_agent_mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: ai_agent_platform
      MYSQL_CHARACTER_SET_SERVER: utf8mb4
      MYSQL_COLLATION_SERVER: utf8mb4_unicode_ci
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - ai_agent_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: ai_agent_backend
    environment:
      DATABASE_URL: mysql+pymysql://root:rootpassword@mysql:3306/ai_agent_platform?charset=utf8mb4
      SECRET_KEY: your-secret-key-change-in-production-12345678
    ports:
      - "8000:8000"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - ai_agent_network
    volumes:
      - ./server/memory_files:/app/memory_files

  frontend:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: ai_agent_frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - ai_agent_network

volumes:
  mysql_data:

networks:
  ai_agent_network:
    driver: bridge
